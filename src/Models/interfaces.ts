// Models
// import { daysOfweek } from "./types";
// Models

export interface I_Product {
  name: string;
  id: string;
  image: string;
  count: number;
  price: number;
}

export interface I_sendTime {
  start: string;
  end: string;
  price: number;
}
